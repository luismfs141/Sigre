import { StyleSheet } from "react-native";

const themeStyles = StyleSheet.create({
    container: {
        backgroundColor: 'white',
    },
    title: {
        textAlign: 'center',
        fontWeight: 'bold'
    },
    subTitle: {
        textAlign: 'center'
    }
});

export { themeStyles }